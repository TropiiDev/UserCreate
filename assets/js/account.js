const imgController = document.querySelector('#profile-img');
const savePreferencesBtn = document.querySelector('.save-btn');

savePreferencesBtn.addEventListener(() => {
  // check if user has an image set, if not hide the image and just show the input
  // save img to imgLink in localStorage
  // add change username, email, password functionality
})
